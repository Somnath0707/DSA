class Solution {
    public int reverseDegree(String s) {
        int temp = 0 ; 
        for(int i = s.length()-1 ; i>= 0 ; i--){
            temp += (26 -(s.charAt(i)-'a')) * (i+1); 
        }
        return temp; 
    }
}