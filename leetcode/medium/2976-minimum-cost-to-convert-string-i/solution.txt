rays.fill(dist ,Integer.MAX_VALUE);
        dist[val-'a'] = 0 ; 

        while(!pq.isEmpty()){
            int temp[] = pq.poll(); 
            char currChar = (char)temp[0]; 
            int cost = temp[1]; 

            if(currChar == find) return cost; 
            for(int ne[] : map.getOrDefault(currChar , new ArrayList<>())){
                char nextChar = (char)ne[0]; 
                int nextCost = ne[1]; 
                int newCost = cost + nextCost; 

                if(dist[ne[0]-'a'] > newCost){
                    dist[ne[0]-'a'] = newCost; 
                    pq.offer(new int[]{ne[0] , newCost});
                }
                
                
            }
        }
        return -1; 
    }
    public long minimumCost(String source, String target, char[] og, char[] notOg, int[] cost) {
        map = new HashMap<>(); 
        int n = og.length; 
        for(int i = 0 ; i < n ; i++){
            char curr = og[i];
            int change = (int)notOg[i];

          