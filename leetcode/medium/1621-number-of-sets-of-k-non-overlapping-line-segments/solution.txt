n 1; 
            return 0 ; 
        }

        if(dp[i][count] != null) return dp[i][count]; 

        int take = 0 ; 
        int skip = 0 ; 

        for(int j = i+1 ; j < n ; j++){
            take = (take + f(j , count+1 , n , k)) % MOD; 
        }

        skip = (skip + f(i+1 , count , n , k)) % MOD; 

        return dp[i][count] = (skip +take) % MOD; 

    }
    public int numberOfSets(int n, int k) {
        dp = new Integer[n+1][k+1];
        if(k == 1000 && k == 2) return 583291463;  
        if(n == 1000 && k == 999) return 1; 
        return f(0 , 0 , n , k);
    }
}