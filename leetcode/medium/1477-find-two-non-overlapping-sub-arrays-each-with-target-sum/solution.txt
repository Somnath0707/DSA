 int[list.size()][4];

        for (int i = list.size() - 1; i >= 0; i--) {
            // add the one with the min value or diff at the last filed till that point like suffix array

            temp[i][0] = list.get(i)[0];
            temp[i][1] = list.get(i)[1];
            temp[i][2] = list.get(i)[2];

            if (i == list.size() - 1) {
                temp[i][3] = temp[i][2];
            } else {
                temp[i][3] = Math.min(temp[i][2], temp[i + 1][3]);
            }
        }

        // then bianry serach each to find the minimum next non over lapping and return the answer

        int ans = Integer.MAX_VALUE;

        for (int i = 0; i < list.size(); i++) {
            int currEnd = temp[i][1];
            int currLen = temp[i][2];

            int next = binarySearch(list, i + 1, list.size() - 1, currEnd);

            if (next < list.size()) {
                ans = Math.min(ans, currLen + temp[next][3]);
            }
        }

        return ans == Integer.MAX_