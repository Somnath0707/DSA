class Solution {
    int MOD = (int)1e9 +7; 
    int dp[][]; 
    public boolean check(int sum , int req){
        int add = 0 ; 
        while(sum > 0){
            add += sum % 10  ; 
            if(add > req) break; 
            sum /= 10 ; 
        }
        return (add == req); 
    }
    public int f(int i , int sum , int [] nums){
        if(i == nums.length) return 1 ; 

        if( sum > 5000 ) return 0 ; 
        if(dp[i][sum] != -1) return dp[i][sum]; 
        int ans = 0 ; 

        